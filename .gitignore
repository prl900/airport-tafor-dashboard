# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Data (large / derived — keep out of git)
data/raw_cache/
data/era5/
data/ifs/
data/nwp/
*.duckdb
*.duckdb.wal
*.grib
*.nc
# The small calibrated model artifacts stay tracked for handoff (load on clone):
# linreg/gbm (sklearn), mlp (GPU PyTorch champion — needs the `dl` extra to load),
# seq2seq (Phase-D baseline). The big duckdb/era5/ifs/nwp/raw_cache stay out (above).

# Node / frontend
frontend/node_modules/
frontend/dist/

# Env / secrets
.env
.cdsapirc

# Inferior Phase-D experiment backbones (kept out of git; regenerate via train_seq)
data/models/gru.joblib
data/models/tcn.joblib
data/models/transformer.joblib

# LaTeX build artifacts
research/*.aux
research/*.log
research/*.out
