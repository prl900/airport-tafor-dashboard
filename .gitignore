# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Data (large / derived — keep out of git)
data/raw_cache/
data/era5/
*.duckdb
*.duckdb.wal
# The small calibrated model artifacts stay tracked for handoff (load on clone):
# linreg/gbm (sklearn), mlp (GPU PyTorch champion — needs the `dl` extra to load),
# seq2seq (Phase-D baseline). The big duckdb/era5/raw_cache stay out (above).

# Node / frontend
frontend/node_modules/
frontend/dist/

# Env / secrets
.env
.cdsapirc
